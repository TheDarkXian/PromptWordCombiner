---
title: PromptWordSplicer 功能实现状态与下一步计划
author: Codex
created_at: 2026-05-02 15:00:00 +08:00
updated_at: 2026-05-02 15:00:00 +08:00
type: report
status: active
---

# PromptWordSplicer 功能实现状态与下一步计划

调查日期：2026-05-02
调查范围：commit `231fd2f` 中涉及的全部代码变更，对照 6 篇规划文档。

---

## 一、总体结论

**6 篇规划文档中提出了 4 大重构目标，在 commit `231fd2f` 中已几乎全部实现，仅剩 2 处小不一致。** 代码质量整体较好，但后续应按架构调查报告的建议进行结构性整理，而非继续堆功能。

---

## 二、各模块实现状态详查

### 2.1 变量系统重构 ✅ 已实现

| 项目 | 文件 | 行号 | 状态 |
|------|------|------|------|
| `VariableSourceType` 联合类型 | `types.ts` | 43-48 | ✅ |
| `ProjectVariable` 接口 | `types.ts` | 50-59 | ✅ |
| `StepOutputBinding`（variableKey + variableLabel） | `types.ts` | 125-128 | ✅ |
| `Project.variables: ProjectVariable[]` | `types.ts` | 14 | ✅ |
| `{{variable_key}}` 插值 | `App.tsx` | 159-187 | ✅ |
| `syncProjectVariables()` 四类变量同步 | `App.tsx` | 189-243 | ✅ |
| Sidebar 三段变量区（输入/局部/结果） | `Sidebar.tsx` | 215-317 | ✅ |
| 结果变量显示来源步骤和状态点 | `Sidebar.tsx` | 108-137 | ✅ |
| 步骤输出写入变量按钮 | `ProjectRunner.tsx` | 464-477 | ✅ |
| 模板步骤「输出到变量」配置区 | `TemplateEditor.tsx` | 930-959 | ✅ |
| 旧字段保留兼容（inputValues/customInputs/stepOutputs） | `types.ts` | 8-12 | ✅ |

### 2.2 步骤交互简化 ✅/⚠️ 基本实现，2 处遗漏

| 项目 | 文件 | 行号 | 状态 |
|------|------|------|------|
| 复制结果按钮提升到步骤头部 | `ProjectRunner.tsx` | 390-407 | ✅ |
| 复制模板按钮在头部 | `ProjectRunner.tsx` | 390-407 | ✅ |
| 去掉 `mode` 字段 | `types.ts` StepOutputBinding | 125-128 | ✅ |
| 「输出到变量」改名 | `TemplateEditor.tsx` | 930 | ✅ |
| 删除正文区重复复制按钮 | `PromptEditor.tsx` | 108 | ⚠️ 仍未删除 |
| 「推荐变量」→「输出」术语统一 | `ProjectRunner.tsx` | 470-471 | ⚠️ 仍是「写入推荐变量」/「更新推荐变量」 |

### 2.3 模板级模型引用 ✅ 已实现

| 项目 | 文件 | 行号 | 状态 |
|------|------|------|------|
| `TemplateModelRef` 接口（id/label/modelCatalogItemId） | `types.ts` | 110-114 | ✅ |
| `StepExecutionConfig.modelRefId` | `types.ts` | 130-135 | ✅ |
| `ProviderConfig` 接口 | `types.ts` | 65-72 | ✅ |
| `ModelCatalogItem` 接口 | `types.ts` | 74-80 | ✅ |
| 模板编辑器模型引用增删改 UI | `TemplateEditor.tsx` | 441-493 | ✅ |
| 步骤选模型引用（非直接选模型） | `TemplateEditor.tsx` | 740-834 | ✅ |
| 执行可用性解析链 | `modelService.ts` | 71-186 | ✅ |
| 执行可用性徽标 | `ProjectRunner.tsx` | 337-341 | ✅ |
| 「执行」按钮 | `ProjectRunner.tsx` | 408-424 | ✅ |
| 执行日志结构 | `types.ts` | 26-41 | ✅ |
| 默认提供商/模型预设 | `constants.ts` | 83-146 | ✅ |
| DeepSeek 预设按钮 | `TemplateEditor.tsx` | 770-794 | ✅ |

### 2.4 模型设置与凭据管理 ✅ 已实现

| 项目 | 文件 | 行号 | 状态 |
|------|------|------|------|
| 设置页 UI 缩放/字号 | `SettingsModal.tsx` | 104-168 | ✅ |
| 提供商配置（API Key/Base URL/启用/开关） | `SettingsModal.tsx` | 170-252 | ✅ |
| 模型目录（绑定提供商/模型名/启用/开关） | `SettingsModal.tsx` | 254-323 | ✅ |
| API Key 密码遮挡 | `SettingsModal.tsx` | 234-236 | ✅ |
| 删除时依赖检查 | `App.tsx` | 748-777 | ✅ |
| 凭据不进模板/项目/备份 | 架构设计 | - | ✅ |
| 旧静态预设→模型目录迁移映射 | `App.tsx` | 30-36 | ✅ |

---

## 三、待修复的 2 处不一致

| # | 文件 | 行号 | 现状 | 应改为 |
|---|------|------|------|--------|
| 1 | `components/PromptEditor.tsx` | 108 | 正文区仍有「复制结果」按钮 | 删除此按钮（头部已有） |
| 2 | `components/ProjectRunner.tsx` | 470 | 「写入推荐变量」「更新推荐变量」 | 「写入输出变量」「更新输出变量」 |

---

## 四、架构调查报告建议的待办事项

以下来自 `代码架构与优化调查报告.md` (2026-05-02)，按优先级排列：

### P1：近期最值得做

| # | 事项 | 说明 |
|---|------|------|
| 1 | 抽离统一插值服务 | 消除 `App.tsx:createInterpolator` 与 `ProjectRunner.tsx:interpolate` 的重复逻辑 |
| 2 | 下沉领域逻辑 | 把 `App.tsx` 中的归一化、变量同步、日志拼接拆到 `domain/` 目录下的纯函数文件 |
| 3 | 抽取共享 AutoResizeTextarea | `PromptEditor`/`ProjectRunner`/`TemplateEditor`/`Sidebar` 各自实现了自适应 textarea，应统一 |

### P2：第二阶段

| # | 事项 | 说明 |
|---|------|------|
| 4 | 拆分大组件 | `TemplateEditor`(910行) 和 `ProjectRunner`(718行) 拆分子组件 |
| 5 | 持久化增加防抖 | 当前直接写盘，编辑频繁时 I/O 压力大，应加 300~800ms 防抖 |
| 6 | 导入数据 schema 校验 | `ioService` 大量使用 `any`，应引入 zod 做运行时校验 |
| 7 | 持久化失败用户提示 | 当前保存失败只 `console.error`，应给 UI 轻提示 |

### P3：中期演进

| # | 事项 | 说明 |
|---|------|------|
| 8 | 引入轻量状态管理层 | props drilling 已较严重，建议 React Context + reducer 或 Zustand |
| 9 | 拆 provider 适配层 | `modelService.ts` 拆成 `executionAvailability/` + `providers/openai.ts` + `providers/anthropic.ts` |
| 10 | 补测试与工程脚本 | 至少补 `typecheck`、`lint`，以及插值/变量同步/合并策略/可用性判断的纯函数测试 |

---

## 五、项目调查报告建议的待办事项

以下来自 `项目调查报告.md` (2026-04-30)：

| # | 事项 | 说明 |
|---|------|------|
| 11 | 统一产品命名 | `index.html` title 仍是 `Game Asset Prompt Splicer`，与当前中文定位不一致 |
| 12 | 清理 `metadata.json` 历史命名痕迹 | 文件有旧项目命名遗留 |
| 13 | 排查中文编码 | PowerShell 终端曾出现乱码，确认仓库统一 UTF-8 |
| 14 | 核实 `index.css` 资源 | build 报 `/index.css doesn't exist at build time`，决定补文件/删引用 |
| 15 | 清理未使用依赖 | 确认是否还需要 `@google/genai` |

---

## 六、变量系统重构方案后续阶段

以下来自 `变量系统重构方案.md`，当前仅完成第一阶段：

| 阶段 | 目标 | 完成情况 |
|------|------|----------|
| Phase 1 | 统一变量模型 + `{{key}}` 引用 + 步骤输出写入变量 | ✅ 已完成 |
| **Phase 2** | 步骤输出绑定：模板步骤声明输出绑定，步骤结果自动写入目标变量 | ⚠️ 部分完成（输出绑定配置已做，但"自动写入"尚未实现） |
| Phase 3 | AI 执行接入：单步执行 OpenAI/Claude，结果自动回填 | ❌ 未开始 |
| Phase 4 | 派生变量：从上游变量自动生成格式化结果 | ❌ 未开始 |

---

## 七、模型设置方案后续阶段

以下来自 `模型设置与凭据管理方案.md`，当前仅完成基础设施：

| 步骤 | 目标 | 完成情况 |
|------|------|----------|
| 1 | 扩展 settings 数据结构 | ✅ |
| 2 | 设置页 UI | ✅ |
| 3 | 模型引用从静态预设切换到模型目录 | ✅ |
| 4 | 旧数据迁移映射 | ✅ |
| **5** | API 连通性测试 | ❌ 未开始 |
| **6** | 单步模型调用真实执行 | ✅ 已实现（`handleRunStep` 调用了 `executeModelText`） |

---

## 八、建议的下一步行动清单（综合排序）

### 立即可做（Bug 修复级，工作量极小）

1. 删除 `PromptEditor.tsx:108` 的重复「复制结果」按钮
2. 将 `ProjectRunner.tsx:470` 的「推荐变量」改为「输出到变量」统一术语

### 近期优先（重构级，对后续开发影响最大）

3. 抽取统一插值服务 `interpolationService.ts`，消除 `App.tsx` 与 `ProjectRunner.tsx` 的插值重复
4. 拆分 `App.tsx`：下沉 `syncProjectVariables`/`normalizeTemplate`/`normalizeProject` 等到 `domain/` 目录
5. 抽取共享 `AutoResizeTextarea` 组件
6. 持久化增加 500ms 防抖 + 失败提示

### 中期（提升工程质量）

7. 拆分 `TemplateEditor.tsx` 为子组件（TemplateMetaPanel / TemplateModelRefPanel / TemplateStepList / TemplateStepCard）
8. 拆分 `ProjectRunner.tsx` 为子组件（ProjectStepCard / ProjectRunLogPanel / ProjectPreviewPanel）
9. 引入 zod 对导入数据做 schema 校验
10. 拆分 `modelService.ts`（executionAvailabilityService + providers/）
11. 引入 React Context + reducer 降低 props drilling

### 产品完善

12. 统一产品命名（index.html title / metadata.json / Tauri productName）
13. 清理 `@google/genai` 等未使用依赖
14. 核实并处理 `index.css` 引用问题
15. 补 `npm run typecheck` 和 `npm run lint` 脚本

### 功能增强

16. 变量系统 Phase 2：步骤输出绑定自动写入变量
17. API 连通性测试按钮（在设置页测试提供商连接）
18. 项目归档 / 模板标签搜索 / 模板版本历史
19. 派生变量（从上游变量自动生成格式化结果）

---

## 九、当前代码健康度评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 功能完整度 | ★★★★★ | 4 大重构目标几乎全部实现 |
| 代码一致性 | ★★★★☆ | 仅 2 处术语/UI 不一致 |
| 可维护性 | ★★★☆☆ | 3 个文件超过 700 行，重复逻辑开始出现 |
| 工程成熟度 | ★★☆☆☆ | 缺少测试、lint、typecheck、状态管理层 |
| 安全性 | ★★★★☆ | 凭据不进模板/项目/备份，API Key 遮挡显示 |

**一句话：现在是做结构性整理的绝佳时机——功能已就位，在继续堆新功能之前先拆大文件、补测试、加类型安全。**

